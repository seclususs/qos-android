[package]
name = "qos_logic"
version = "0.1.0"
edition = "2024"

[lib]
name = "qos_logic"
crate-type = ["staticlib"]

[dependencies]
libc = "0.2"
log = "0.4"
android_logger = "0.15"
rustix = { version = "1.1", features = ["fs", "event", "mm", "param"] }